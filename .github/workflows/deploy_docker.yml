name: Build and push Docker image

on:
  push:
    branches: [ "main" ]

jobs:
  get_vault_secrets:
    runs-on: ubuntu-latest
    steps:
    
      - name: Get Vault secret
        id: vault
        uses: hashicorp/vault-action@v2
        with:
          url: ${{ vars.VAULT_ADDR }}
          token: ${{ secrets.VAULT_TOKEN }}
          secrets: blagoy-secret/data/docker DOCKER_PASSWORD

      - name: Set output vars
        run: echo "::set-output name=DOCKER_PASSWORD::${{ steps.vault.outputs.DOCKER_PASSWORD }}"
  
  push_to_registry:

    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    needs: get_vault_secrets
    steps:
      
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ steps.vault.outputs.DOCKER_PASSWORD }}
          password: ${{ needs.get_vault_secrets.outputs.DOCKER_PASSWORD }}
          
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO_NAME }}
          tags: latest
          labels: latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  server_update:
    needs: push_to_registry
    name: Update server by ssh
    runs-on: ubuntu-latest
    steps:
      - name: Connect and run script
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          port: ${{ secrets.SERVER_PORT }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          script_stop: true
          script: bash /home/administrator/app/deploy.sh
